<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>fr.jixter</groupId>
    <artifactId>dailypull-imagegen</artifactId>
    <version>1.0.0</version>
    <name>DailyPull Image Generator</name>
    <description>Outil de génération d'images pour les cartes DailyPull via l'API 1minai</description>

    <properties>
        <java.version>25</java.version>

        <!--
          ops#1980 — surcharge AU-DESSUS de ce que le BOM Spring Boot 4.1.0 pinne.
          Ce n'est pas une préférence : sans elle la mesure `cve-deps-parc.py` reste ROUGE au
          seuil HIGH sur ce dépôt. La version pinnée par le BOM est nommée en face, pour que la
          surcharge devienne SUPPRIMABLE dès que Boot la rattrape — une surcharge dont on ne
          sait plus ce qu'elle dépasse ne se retire jamais.

          tomcat 11.0.22 (BOM Spring Boot 4.1.0) -> 11.0.25 : `tomcat-embed-core` porte encore
            en 11.0.22 les avis fermés par 11.0.25. 11.0.25 est la version corrigée nommée par
            les avis OSV, et elle EST publiée sur Maven Central : l'index solrsearch de
            search.maven.org s'arrêtait à 11.0.22 le 2026-09-07, la disponibilité se mesure donc
            sur `maven-metadata.xml`, jamais sur l'index de recherche.
        -->
        <tomcat.version>11.0.25</tomcat.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>3.6.0</version>
                <configuration>
                    <java>
                        <googleJavaFormat/>
                    </java>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
